import styled from "@emotion/styled";

const Form = styled.form({
  marginTop: "75px",
  display: "flex",
  gap: "1.2em",
  alignItems: "center",
  flexDirection: "column",
});

export default Form;
